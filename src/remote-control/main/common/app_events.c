#include "app_events.h"

/**
 * @file app_events.c
 * @brief Application Event System Implementation
 * 
 * Declares the application event base for the ESP event loop.
 */

ESP_EVENT_DEFINE_BASE(APP_EVENTS);
